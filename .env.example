# Example environment file
# Note: Most runtime settings are now stored in PostgreSQL via the Admin UI.
# This file is used primarily for local development and CI. Do not commit real secrets.

# Application
PORT=3000
HEALTH_CHECK_PORT=3001
TIMEZONE=UTC
LOG_LEVEL=info

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=frijolebot
POSTGRES_PASSWORD=frijolebot
POSTGRES_DATABASE=frijolebot
POSTGRES_SSL=false

# Redis
REDIS_URL=redis://127.0.0.1:6379/1

## Discord (used only for initial testing; final values saved in Admin UI)
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=
DISCORD_ADMIN_CHANNEL=

## WhatsApp feature flag is managed in Admin UI; keep disabled by default here
# WHATSAPP_ENABLED=false

## Admin notify token for internal health notifications
ADMIN_NOTIFY_TOKEN=dev-notify-token

## Rate Limiting (defaults used if omitted; configurable via Admin UI)
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=5
# RATE_LIMIT_CLEANUP_INTERVAL=300000
