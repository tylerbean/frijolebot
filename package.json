{
  "name": "discord-link-bot",
  "version": "1.0.0",
  "description": "Discord bot that monitors channels for links and sends them to n8n for processing",
  "main": "discord-link-bot.js",
  "scripts": {
    "start": "node discord-link-bot.js",
    "dev": "nodemon discord-link-bot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "discord",
    "bot",
    "n8n",
    "webhook",
    "link-monitoring"
  ],
  "author": "Tyler Bean",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.14.1",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
